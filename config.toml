[server]
# Allow large model / dataset uploads (default is 200 MB)
maxUploadSize = 2048
# Stop the file-watcher walking into installed packages like transformers
# (this is what produced the long "torchvision not found" warning trail).
fileWatcherType = "none"

[theme]
base = "light"
primaryColor = "#3b6fff"
